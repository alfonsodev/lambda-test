You need to have aws crendentials already in place   `~/.aws/crednetials`  
```
export AWS_REGION=eu-west-1  
export AWS_IAM_ROLE=arn:aws:iam::111111111111:role/lambda_deploy
npm install  
npm run deploy  
```

